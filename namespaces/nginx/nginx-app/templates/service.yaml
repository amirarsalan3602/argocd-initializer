apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name  }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "my-nginx-chart.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80

